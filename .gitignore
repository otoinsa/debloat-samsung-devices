# firmware downloads and patched images — these are large binary files
tmp/firmware/*
tmp/patched/*

# keep the directory structure and device notes
!tmp/firmware/.gitkeep
!tmp/patched/.gitkeep
!tmp/DEVICE_INFO.md
